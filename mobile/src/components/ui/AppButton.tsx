/**
 * AppButton — alias for PremiumButton.
 * Provides a clean "App*" naming convention for the design system primitives.
 * See PremiumButton for full prop documentation.
 */
import PremiumButton from "./PremiumButton";
export default PremiumButton;
/**
 * AppCard — alias for PremiumCard.
 * Provides a clean "App*" naming convention for the design system primitives.
 * See PremiumCard for full prop documentation.
 */
import PremiumCard from "./PremiumCard";
export default PremiumCard;